# Foreglow — Kitty Terminal Theme

## Installation

1. Copy `foreglow.conf` to your Kitty config directory:
   ```bash
   cp foreglow.conf ~/.config/kitty/foreglow.conf
   ```

2. Add the include to your `kitty.conf`:
   ```conf
   include foreglow.conf
   ```

3. Restart Kitty.
